# secrets/

Place your Firebase service account JSON file here:

  firebase-service-account.json

This entire folder is git-ignored. Never commit these files.
The app loads the file at `apps/api/secrets/firebase-service-account.json` automatically.
